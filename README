# Simple ansible playbook which run voting-app (https://github.com/docker/example-voting-app) 
# docker containters structure on EC2 machine.
# To run type: ansible-playbook -i ./inventory create_ec2_with_app.yml
